#!/bin/bash

# magikos:summary=Returns true if all of the named packages are installed on the system (or false if any of them are missing).
# magikos:args=<packages...>

source magikos-pkg-backend

pkg_installed "$@" || exit 1

exit 0
