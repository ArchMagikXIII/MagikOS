#!/bin/bash

# magikos:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/magikos/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! magikos-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
