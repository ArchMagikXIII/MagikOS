#!/bin/bash

# magikos:summary=Launch 1Password or start its installer when missing.

set -e

if magikos-cmd-present 1password; then
  exec setsid uwsm-app -- 1password
else
  exec magikos-launch-floating-terminal-with-presentation magikos-install-service-1password
fi
