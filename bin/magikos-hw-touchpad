#!/bin/bash

# magikos:summary=Print the detected touchpad or trackpad device name

device=$(swaymsg -t get_seats -r | jq -r '[.[].devices[] | select(.type == "pointer") | .name | select(test("touchpad|trackpad"; "i"))] | first // empty')
[[ -n $device ]] && echo "$device"
