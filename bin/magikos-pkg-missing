#!/bin/bash

# magikos:summary=Returns true if any of the named packages are missing from the system (or false if they're all there).
# magikos:args=<packages...>

source magikos-pkg-backend

if pkg_installed "$@"; then
  exit 1
fi

exit 0
