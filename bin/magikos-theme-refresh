#!/bin/bash

# magikos:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.local/state/magikos/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  MAGIKOS_THEME_SKIP_BACKGROUND=1 magikos-theme-set "$(cat $THEME_NAME_PATH)"
fi
