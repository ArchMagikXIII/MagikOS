#!/bin/bash

# magikos:summary=Shut down after closing application windows
# magikos:examples=magikos shutdown | magikos system shutdown
# magikos:aliases=magikos shutdown

# Schedule the shutdown in the user manager so closing the terminal's systemd
# scope cannot terminate it before it runs.
systemd-run --user --collect --quiet --on-active="2s" --timer-property=AccuracySec=100ms systemctl poweroff --no-wall || exit 1

magikos-osd -i shutdown -m "Shutting down" -d 5000

magikos-state clear re*-required

# Now close all windows
magikos-hyprland-window-close-all
sleep 1 # Allow apps like Chrome to shutdown correctly
