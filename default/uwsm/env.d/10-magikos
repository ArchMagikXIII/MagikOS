# Changes require a restart to take effect.

# UWSM doesn't source /etc/profile.d, so pull the env bootstrap in directly.
[ -r /usr/share/magikos/default/bash/env-bootstrap ] && . /usr/share/magikos/default/bash/env-bootstrap

# Magikos session defaults. Users can override these in ~/.config/uwsm/default
# or, preferably, ~/.config/uwsm/env.d/*.
if [ -f "${MAGIKOS_PATH%/}/default/uwsm/default" ]; then
  . "${MAGIKOS_PATH%/}/default/uwsm/default"
else
  export TERMINAL=xdg-terminal-exec
  export EDITOR="magikos-launch-editor --inline"
fi

# Optional user overrides kept for compatibility with older installs.
[ -f "$HOME/.config/uwsm/default" ] && . "$HOME/.config/uwsm/default"

# Activate mise if present on the system
magikos-cmd-present mise && eval "$(mise activate bash --shims)"
